spring:
  application:
    name: raymoore-xyz
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  flyway:
    default-schema: public
    schemas: public,madisonsc
  sql:
    init:
      mode: never

app:
  api-secret: ${API_SECRET}
